# WORKFLOW - Purple Book Generator

## Working Methods
- Source of truth script:
  - _DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py
- Compatibility detector script (dump-vs-vanilla, changed/added/removed):
  - SCRIPT-GenPurpleBookCompat.py
- Preferred launch command (repo root, keeps existing workflow stable):
  - c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe SCRIPT-PurpleBookGenerator.py --no-sync-game-mod --no-sync-activebuild
- Preferred dump-diff command for compat detection:
  - c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe SCRIPT-GenPurpleBookCompat.py --dump-diff --dump-dir "C:/Users/rft30/AppData/Roaming/7DaysToDie/Saves/<save>/<world>/ConfigsDump" --detail-limit 20
- Preferred Purple-Book-scoped count command:
  - c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe SCRIPT-GenPurpleBookCompat.py --dump-diff-purplebook --dump-dir "C:/Users/rft30/AppData/Roaming/7DaysToDie/Saves/<save>/<world>/ConfigsDump" --detail-limit 0
- Optional direct command (same result):
  - c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py --no-sync-game-mod --no-sync-activebuild
- Live-game sync command for HUDPlus PurpleBook target:
  - c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe SCRIPT-PurpleBookGenerator.py --sync-game-mod --no-sync-activebuild --game-mod-windows "c:\Program Files (x86)\Steam\steamapps\common\7 Days To Die\Mods\AGF-HUDPlus-PurpleBook-v2.0.1\Config\XUi_InGame\windows.xml"
- Output target default:
  - _DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Config/XUi_InGame/windows.xml
- Non-EnhancedAGF fallback header objects (Title/DayTime/Location) are generator-managed in `emit_window`; adjust there instead of hand-editing live game `windows.xml`.

## Change History
- 2026-06-28:
  - Moved AGF-PurpleBookGenerator-v0.0.1 from 01_Draft to _DLL-Projects.
  - Updated root launcher SCRIPT-PurpleBookGenerator.py to point at _DLL-Projects path.
  - Updated generator defaults to derive paths from script location.
  - Added out-mod directory resolution fallback for _DLL-Projects and legacy 01_Draft.
  - Updated key repo guidance files to new source-of-truth path.
  - Ran no-sync validation successfully after relocation.
- 2026-06-29:
  - Confirmed source-of-truth remains `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py`.
  - Deleted stale duplicate draft folder:
    - `01_Draft/AGF-PurpleBookGenerator-v0.0.1`
  - Validation evidence:
    - DLL script is newer than deleted draft copy (DLL LastWriteTime: 2026-06-29 15:18:39, draft LastWriteTime: 2026-06-28 13:00:33).
  - Added generator emission for non-EnhancedAGF fallback block under Schematics window:
    - `<conditional evaluator="client"><if cond="mod_loaded('AGF-NoEAC-EnhancedAGF') == false">...`
    - `agfPbFallbackTitle`, `agfPbFallbackDayTime`, `agfPbFallbackLocation`
  - Included current day/time internals used in live testing (icon + left-aligned label composite).
  - Purpose: ensure fallback header UI is reproduced on every generator run.
  - Ran live sync for PurpleBook files and verified fallback header exists in live game windows.xml.
  - First sync attempt using default `--sync-game-mod` target warned missing path because default target resolves to `AGF-PurpleBookGenerator-v0.0.1` mod folder.
  - Re-ran with explicit `--game-mod-windows` pointing to `AGF-HUDPlus-PurpleBook-v2.0.1`, which successfully synced:
    - `Config/XUi_InGame/windows.xml`
    - `Config/XUi_InGame/xui.xml`
    - `Config/gameevents.xml`
    - `Config/Localization.csv`
  - Restored Purple Book localization key behavior in generator-managed output:
    - Added `agf0PurpleBookButton` row with multilingual values.
    - Changed `agf0PurpleBookButtonTooltip` from "Crafting List" back to "Purple Book" multilingual values.
    - Updated localization merge controls so both keys are treated as generator-authored keys.
  - Regenerated and synced live PurpleBook files; confirmed live Localization.csv now includes both expected key rows.
  - Corrected fallback title label key in generator output from `xuiSchematics` to `agf0PurpleBookButton` so live header title resolves to Purple Book text instead of showing missing key text.
  - Fixed live progression tracking sync gap:
    - Root cause: `--sync-game-mod` copied windows/xui/gameevents/localization but did not copy `Config/buffs.xml`.
    - Added `buffs.xml` sync for both `--sync-game-mod` and `--sync-activebuild` paths in generator source.
    - Re-ran explicit live sync to HUDPlus PurpleBook target and confirmed `synced game mod buffs` is now emitted.
    - Post-sync validation: live windows references and live buffs writers match for all categorized crafting `Check` CVars (428/428), with both `onSelfBuffStart` and `onSelfBuffUpdate` coverage.
  - Added game-file driven buffs contract validation inside generator run:
    - Uses parsed progression crafting skills (`name` + `max_level`) as source-of-truth expectations.
    - Verifies `Config/buffs.xml` includes per-category total `crafting...Check`, start/update progression seeding up to each category max, and full UI cvar coverage used by windows.xml.
    - Generator run now fails early if this contract is broken (prevents syncing partial/stale category tracking).
  - Expanded generated gameevents recalc hooks for first-load and respawn paths:
    - `game_first_spawn`
    - `game_on_spawn`
    - `game_on_respawn_none`
    - `game_on_respawn_default`
    - `game_on_respawn_injured`
    - `game_on_respawn_permanent`
    - Each appends `AddBuff agfRecalculate` so checklist/category CVars refresh on those lifecycle events.
  - Switched buffs pipeline to full generator emission (no static buffs authoring dependency):
    - Added `_emit_buffs_recalculate_patch(skills)` that builds full `Config/buffs.xml` from parsed progression categories and unlock thresholds.
    - Added per-skill threshold derivation from display unlock levels (`*CheckN` CVars) and emits deterministic update toggles for in-range/out-of-range transitions.
    - Generator now writes `Config/buffs.xml` every run, with autobackup support via `buffs.prewrite.*.xml`.
  - Preserved legacy book/perk recalc behavior while generating crafting recalc logic:
    - Added seed-driven preservation of non-crafting `triggered_effect` rows from `buff[@name='agfRecalculate']`.
    - Added `--seed-buffs` argument and fallback ordering to source non-crafting recalc effects.
    - Fallback now prefers original `buffs.pre-recreate.source.*` backups before current generated output to avoid AGF flag loss.
  - Hardened buffs contract validation to generated-output parity checks before write/sync:
    - Validates total `*Check` coverage, start/update max coverage to progression max, UI token coverage, and exact threshold-set parity per skill.
    - Generator run now fails before deployment if generated buffs contract does not match progression/windows expectations.
  - Refactored buffs emission to mirror original semantic behavior layers instead of a single collapsed block:
    - `/buffs` append for `agfRecalculate` startup groups.
    - `buffStatusCheck02` append for preserved AGF book runtime deltas (`bookProgression`).
    - `buffStatusCheck02` append for generated crafting runtime deltas (`craftingChecker`).
  - Fixed threshold runtime semantics by row type:
    - Tiered rows use range-window behavior (`>= level` and `< next`) plus clear-on-next.
    - Non-tiered rows use persistent unlock-flag behavior (`>= level` / `< level`) with no next-window bounds.
  - Validation result after semantic fix:
    - No-sync generator run passed.
    - Full triggered_effect usage parity vs original seed backup reached exact match:
      - missing CVar names: 0
      - extra CVar names: 0
      - mismatched CVar usage signatures: 0
  - Fixed legacy paging-header key migration for EnhancedAGF compatibility:
    - `checklistSchematicsTitle` now maps to `agf0PurpleBookButton` (all-caps header text) instead of `agf0PurpleBookButtonTooltip`.
    - Intent: ensure legacy header references resolve to the all-caps Purple Book title key rather than mixed-case tooltip text.
  - Added quality-tier compatibility guard driven by game `qualityinfo.xml`:
    - New `--qualityinfo` argument (default: game `Data/Config/qualityinfo.xml`).
    - Generator reads playable quality tier count from `<quality key="...">` and verifies it matches current tier rendering assumptions.
    - Generator now stops early on mismatch to prevent silent Q-tier regressions in tiered rows and bars.
  - Added progression quality-row validation:
    - Every `has_quality` `display_entry` must match the detected quality tier count.
    - Generator fails fast if a quality row unlock count diverges from qualityinfo tier model.
  - Live test deployment to game Mods path completed with pre-sync backup:
    - Created backup at:
      - `C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v2.0.1/Config/_AUTOBACKUP/pre-live-sync.20260629-151213`
    - Backed up before sync:
      - `Config/XUi_InGame/windows.xml`
      - `Config/XUi_InGame/xui.xml`
      - `Config/gameevents.xml`
      - `Config/buffs.xml`
      - `Config/Localization.csv`
    - Ran live sync command:
      - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py --sync-game-mod --no-sync-activebuild --game-mod-windows "c:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v2.0.1/Config/XUi_InGame/windows.xml"`
    - Sync confirmed for all PurpleBook config targets:
      - windows.xml, xui.xml, gameevents.xml, buffs.xml, Localization.csv
    - Post-sync localization check on live file:
      - `mojibake_count=0`
  - EnhancedAGF live-title fallback fix for `XUISCHEMATICS` display:
    - Root cause: EnhancedAGF title path can resolve to localization key `xuiSchematics`; when missing, the raw key was rendered in UI.
    - Generator now emits a managed localization alias row:
      - `xuiSchematics` => `PURPLE BOOK` (with curated language translations matching Purple Book title set).
    - Added `xuiSchematics` to generator-managed localization sets:
      - forced generated keys
      - allowed preservation-change keys
      - static localization rows authored each run
    - Created second pre-sync backup at:
      - `C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v2.0.1/Config/_AUTOBACKUP/pre-live-sync.20260629-151747`
    - Live sync rerun completed for all PurpleBook config files.
    - Live validation:
      - `Localization.csv` contains `xuiSchematics` row with Purple Book values.
      - `mojibake_count=0`.
  - 2026-07-01:
    - Added dump-diff mode to `SCRIPT-GenPurpleBookCompat.py` to compare ConfigsDump vs vanilla.
    - New mode reports three buckets per tracked file (`progression.xml`, `items.xml`, `recipes.xml`):
      - changed (same keyed entity, different content)
      - added (present in dump, absent in vanilla)
      - removed (present in vanilla, absent in dump)
    - Added comment-marker parsing (`... by: "<ModName>"`) to surface source-mod hints per changed/added entity and file-level marker counts.
    - Validation run completed against:
      - `C:/Users/rft30/AppData/Roaming/7DaysToDie/Saves/Dishong Challenge (3. Desert)/DTC Test2/ConfigsDump`
    - Validation totals observed in run:
      - changed: 1421
      - added: 12
      - removed: 18
    - Expanded tracked dump coverage to include `item_modifiers.xml` (`item_modifier@name`).
    - Validation rerun totals after expansion:
      - changed: 1528
      - added: 12
      - removed: 18
    - Added `--dump-diff-purplebook` mode to report Purple-Book-relevant counts only.
    - Purple-Book-relevant field scope:
      - `progression.xml`: `crafting_skill` `RecipeTagUnlocked` + `max_level`
      - `items.xml`: `UnlockedBy`, `CustomIcon`, `Tags`, and recipe-presence craftability
      - `recipes.xml`: `tags`, learnable-state, and `always_unlocked`
      - `item_modifiers.xml`: `UnlockedBy`
    - Validation run against Dishong Challenge ConfigsDump with `--detail-limit 0`:
      - changed: 35
      - added: 7
      - removed: 14
    - `item_modifiers.xml` in Purple-Book scope currently reports no `UnlockedBy` deltas (0/0/0), despite broader structural marker activity.
    - Added dump-generated rewrite mode to `SCRIPT-GenPurpleBookCompat.py`:
      - `--dump-generate-purplebook-rewrite --dump-dir <ConfigsDump> --skills craftingWorkstations,craftingVehicles,craftingSeeds`
      - Emits generated conditional blocks for Purple Book windows remaps directly from progression diff data (vanilla vs dump).
      - Emits generated buffs cvar startup/update block for newly introduced threshold levels only.
    - Verified Dishong Forge Ahead generation output now places lockpick before workbench from data-driven section mapping (`L7=resourceLockPick`, `L10=workbench`) instead of manual swap edits.
    - Improved rewrite generator cleanup behavior for removed progression entries:
      - Generated windows block now emits `<remove ...>` lines for stale `itemIcon` / `itemtypeicon` nodes when dump rows contain fewer icons than vanilla rows.
      - Generated windows block now emits `<remove ...>` lines for stale sections/entries when dump progression has fewer rows than vanilla.
    - Updated uniform-level behavior (Seeds):
      - Non-quality section grouping now follows unlock level.
      - When many seed rows share the same unlock level (for example all level 1), generator merges them into one section and combines icons in that section.
      - Applies to both compact strip (`checklistSeeds`) and zoomed Seeds grid (`zoomedSeeds`).
    - Validation run (no live sync) using ConfigsDump inputs written to temp out-mod:
      - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/temp/seeds-grouping-check/`
      - Confirmed output shape for collapsed dump case:
        - `zoomedSeeds/checklistSeeds` renders `rows="1" cols="1" cell_width="600"`
        - combined section uses `craftingSeedsCheck1` and `text="/1"`
      - XML parse check passed for generated temp `windows.xml`.
    - Confirmed rewrite coverage now includes multi-icon rows:
      - Generation rewrites all emitted `itemIcon[N]` / `itemtypeicon[N]` slots from dump order and emits stale-slot cleanup removes.
    - Live preview apply method (generator-driven):
      - Generate UTF-8 artifact output and replace only the `mod_loaded('AGF-HUDPlus-PurpleBook')` conditional blocks in live Dishong `windows.xml` and `buffs.xml`.
      - Validation requirement after replace: XML parse must pass and PurpleBook region tag sanity must report balanced `if` and `conditional` tags.
    - Added source-generator compare rewrite mode to `SCRIPT-GenPurpleBookCompat.py`:
      - `--dump-generate-purplebook-rewrite-from-generator --dump-dir <ConfigsDump> --skills craftingVehicles,craftingWorkstations,craftingSeeds`
      - Runs source generator twice (vanilla config inputs vs ConfigsDump inputs), then emits changed-only Dishong compatibility rewrite blocks from generated output diff.
      - Uses source generator script path:
        - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py`
      - Temp compare outputs:
        - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/temp/fullgen-compare/vanilla/`
        - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/temp/fullgen-compare/dump/`
        - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/temp/fullgen-compare/rewrite-from-generator.txt`
      - Source-generator out-mod caveat:
        - Out-mod is resolved relative to `_DLL-Projects`; target folder must exist before run.
      - Extraction caveat:
        - Source-generated `windows.xml` root is `AGF-PurpleBookGenerator` with patch operations and embedded `window` nodes; extraction XPath must target `//window[@name='Schematics']...` (not `/windows/window...`).
    - 2026-07-01 (live dump-sync requested):
      - Created full backup of live game PurpleBook mod before push:
        - `C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/temp/AGF-HUDPlus-PurpleBook-v3.0.0-backup-20260701-205759`
      - Ran source generator directly from ConfigsDump inputs and pushed to live game PurpleBook mod:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py --progression "<dump>/progression.xml" --qualityinfo "<dump>/qualityinfo.xml" --items "<dump>/items.xml" --blocks "<dump>/blocks.xml" --recipes "<dump>/recipes.xml" --sync-game-mod --no-sync-activebuild --game-mod-windows "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v3.0.0/Config/XUi_InGame/windows.xml"`
      - Generator reported live sync success for:
        - `Config/XUi_InGame/windows.xml`
        - `Config/XUi_InGame/xui.xml`
        - `Config/gameevents.xml`
        - `Config/buffs.xml`
        - `Config/Localization.csv`
      - Post-sync validation:
        - XML parse check passed for live `windows.xml`, `xui.xml`, `gameevents.xml`, and `buffs.xml`.
    - 2026-07-01 (gas can bundle icon correction):
      - Root cause found in compare-mode rewrite extraction:
        - Old logic collapsed icon mapping to one `tag` via `tooltip_key or sprite`, which can write invalid sprite values for bundle-like entries.
        - Example: Vehicles row for gas can bundle should be `sprite=ammoGasCan` + `tooltip_key=ammoGasCanBundle`.
      - Fixed `SCRIPT-GenPurpleBookCompat.py` compare mode to track icon pairs `(sprite, tooltip_key)` and emit separate set operations.
      - Live Dishong compat hotfix applied to vehicles row 35 icon #2 in both checklist and zoomed views:
        - `@sprite` changed from `ammoGasCanBundle` to `ammoGasCan`
        - `@tooltip_key` kept as `ammoGasCanBundle`
      - Post-fix validation:
        - XML parse passed for live Dishong `windows.xml`.
    - 2026-07-01 (live push for seeds unlock-level grouping test):
      - User-requested live deployment executed from ConfigsDump inputs with sync enabled:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe SCRIPT-PurpleBookGenerator.py --progression "<dump>/progression.xml" --qualityinfo "<dump>/qualityinfo.xml" --items "<dump>/items.xml" --blocks "<dump>/blocks.xml" --recipes "<dump>/recipes.xml" --sync-game-mod --no-sync-activebuild --game-mod-windows "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v3.0.0/Config/XUi_InGame/windows.xml"`
      - Live sync confirmed for all generated PurpleBook files:
        - `Config/XUi_InGame/windows.xml`
        - `Config/XUi_InGame/xui.xml`
        - `Config/gameevents.xml`
        - `Config/buffs.xml`
        - `Config/Localization.csv`
      - Post-sync validation:
        - XML parse checks passed for live `windows.xml`, `xui.xml`, `gameevents.xml`, and `buffs.xml`.
        - mojibake check passed with `MOJIBAKE_COUNT:0` on live `Localization.csv`.
        - Seeds grouped output visible in live `windows.xml` (`zoomedSeeds` uses `rows="1" cols="1" cell_width="600"` with `/1`).
    - 2026-07-01 (generator behavior updates for current Dishong dump test):
      - Cosmetic Lock Icon exclusion:
        - Added generator-side suppression for `ItemTypeIcon=scrap` (and `ui_game_symbol_scrap`) during `parse_items`, so cosmetic lock marker overlays are not emitted in generated Purple Book UI.
      - Zoomed single-section icon packing:
        - Updated `emit_qsection_single` to wrap icons at up to 6 per row when a section has many icons, with adaptive icon sizing/row centering.
        - Verified collapsed Seeds section now renders as two rows of six icons in one wide section for the all-level-1 dump case.
      - Wrapped-row size correction (generator-only):
        - Updated `emit_qsection_single` sizing math to reduce vertical gap first on multi-row sections so icon size stays at `50` when it still fits.
        - Revalidated with dump-driven run (`craftingSeeds max=1` case) and confirmed live `zoomedSeeds` collapsed section now emits `size="50,50"` in both wrapped rows.
      - Seeds zoomed box width tightening (generator-only):
        - Updated `emit_zoomed_view` Seeds `cell_width` calculation to derive from widest rendered row (6-per-row cap) plus chip-clearance margin, instead of `max_icons * 50`.
        - Collapsed Seeds (`/1`, 12 icons) now emits `cell_width="440"` (was `600`) with icon anchors starting at `x=55`/`y=0,-50`.
        - Live dump-driven sync completed to `AGF-HUDPlus-PurpleBook-v3.0.0` and XML parse + mojibake checks passed.
      - Column reorder correctness fix (generator-only):
        - Updated TAB_ORDER height calculation for Seeds to use grouped compact-strip height (`_group_slots_by_unlock_level`) instead of raw display-entry approximation.
        - Non-tiered order now reflects visible compact heights in dump mode (example: Traps -> Seeds -> Vehicles -> Medical).
      - Wiring-55 style Seeds zoom sizing (generator-only):
        - Added `section_height` support to `emit_qsection_single` and applied it in Seeds zoom mode.
        - Seeds collapsed section now uses `cell_height="125"` with icon rows at `y=-8` and `y=-66` (matching Wiring 101 level-55 section geometry pattern).
        - Row overflow rule preserved: section icon rows still use 6-per-row; icon counts above 12 naturally add additional rows.
      - Tab column ordering policy update:
        - Tiered-only columns are now sorted by computed height descending (longest to shortest).
        - Non-tiered columns are sorted by computed height ascending (shortest to longest).
      - Live deployment and checks:
        - Synced all generated PurpleBook files to live `AGF-HUDPlus-PurpleBook-v3.0.0`.
        - XML parse checks passed for live `windows.xml`, `xui.xml`, `gameevents.xml`, `buffs.xml`.
        - mojibake check passed with `MOJIBAKE_COUNT:0`.
        - Live `windows.xml` confirmed no `ui_game_symbol_scrap` markers and confirmed Seeds `rows="1" cols="1"` plus 6-per-row wrapping positions.
    - 2026-07-01 (vanilla-generation compat determination for Dishong):
      - Ran source-generator rewrite diff mode against active Dishong ConfigsDump:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/SCRIPT-GenPurpleBookCompat.py --dump-generate-purplebook-rewrite-from-generator --dump-dir "C:/Users/rft30/AppData/Roaming/7DaysToDie/Saves/Dishong Challenge (3. Desert)/DTC Test2/ConfigsDump"`
      - Saved generated artifact to workspace temp:
        - `temp/PurpleBookCompat-VanillaToDump-Dishong.txt`
      - Generated windows rewrite requirements:
        - `craftingWorkstations`: `set=74`, `remove=0`, `warnings=0`
        - `craftingVehicles`: `set=38`, `remove=28`, `warnings=0`
        - `craftingSeeds`: `set=34`, `remove=18`, `warnings=14`
      - Generated buff cvar requirements:
        - Workstations: `1,5,7,27,28,36,45`
        - Vehicles: `15,35,50`
        - Seeds: `1`
      - Cross-check result:
        - Dishong `buffs.xml` already contains the required cvar start/update block; main stale area remains Dishong `windows.xml` rewrite block.
      - Seeds caveat from generator notes:
        - `set` operations for `itemIcon[2..8]` in collapsed `checklistSeeds Section1` and `zoomedSeeds entry[1]` can no-op if those nodes are absent in baseline output.
        - Safer patching method for Seeds collapse rewrites is remove+append of full section/entry nodes (or append missing icon nodes before set).
      - Applied generated windows compatibility block to live Dishong file:
        - `C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/zzzAGF-Special-Compatibilities-v5.0.0/Config/XUi_InGame/ModPatches/DishongTowerChallenge/windows.xml`
        - Replaced legacy gated block (`mod_loaded('AGF-HUDPlus-PurpleBook') and mod_loaded('__agf_pb_legacy_compat_enable__')`) with generated block (`mod_loaded('AGF-HUDPlus-PurpleBook')`).
      - Post-apply validation:
        - XML parse passed.
        - Conditional/if tag counts balanced (`conditional 4/4`, `if 5/5`).
        - Legacy opt-in flag marker count is `0`.
        - VS Code diagnostics reported no XML errors for the edited file.
    - 2026-07-01 (vanilla live-state prep for compat testing):
      - Synced live PurpleBook from vanilla game `Data/Config` inputs (no dump inputs) to force a clean vanilla baseline in live folder:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py --progression "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/progression.xml" --qualityinfo "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/qualityinfo.xml" --items "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/items.xml" --blocks "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/blocks.xml" --recipes "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/recipes.xml" --sync-game-mod --no-sync-activebuild --game-mod-windows "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v3.0.0/Config/XUi_InGame/windows.xml"`
      - Generator sync confirmed all live PurpleBook targets updated:
        - `Config/XUi_InGame/windows.xml`, `Config/XUi_InGame/xui.xml`, `Config/gameevents.xml`, `Config/buffs.xml`, `Config/Localization.csv`
      - Post-sync validation:
        - XML parse checks passed for live windows/xui/gameevents/buffs.
        - Mojibake check passed with `MOJIBAKE_COUNT:0` on live Localization.csv.
    - 2026-07-01 (compat correction after vanilla-base test):
      - User-reported regression: current Dishong PurpleBook compatibility block was visually off on vanilla baseline.
      - Root pattern: changed-only source-generator diff block (`Auto-generated from source-generator vanilla-vs-dump output diff`) used collapsed Seeds assumptions that were unstable against vanilla section structure.
      - Corrective action: replaced live Dishong PurpleBook windows conditional with progression-diff block (`Auto-generated from progression dump diff`) from:
        - `SCRIPT-GenPurpleBookCompat.py --dump-generate-purplebook-rewrite --dump-dir <ConfigsDump>`
      - Resulting live compat windows block now rewrites Seeds/Vehicles/Workstations against vanilla-style section indexes (including Seeds sections 1..10 and zoomed entries 1..10) instead of collapsed single-section assumptions.
      - Validation after replacement:
        - XML parse passed.
        - Conditional/if tags remained balanced.
        - VS Code diagnostics reported no XML errors.
    - 2026-07-01 (strict parity restoration to match known-good overwrite):
      - User required compatibility behavior to match known-good overwrite, not just progression/icon rewrites.
      - Root pattern confirmed: both changed-only and progression-diff rewrite modes can drift from overwrite parity when layout/structure changed (not only values).
      - Implemented strict-parity windows method:
        - Source extracted from fullgen dump output window definitions at:
          - `_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/temp/fullgen-compare/dump/Config/XUi_InGame/windows.xml`
        - Generated conditional artifact:
          - `temp/PurpleBookCompat-StrictParity-Dishong.txt`
        - Applied to live Dishong compat file by replacing the `mod_loaded('AGF-HUDPlus-PurpleBook')` conditional block.
      - Strict-parity method details (for Workstations/Vehicles/Seeds):
        - Set container/grid geometry attributes (including collapsed Seeds layout).
        - Remove existing compact `SectionN` and zoomed `entry[N]` nodes.
        - Append full section/entry nodes from fullgen dump output to mirror overwrite structure exactly.
        - Set title fill + level text from dump output (`/50` for Workstations/Vehicles and `/1` for Seeds).
      - Validation after strict-parity apply:
        - XML parse passed.
        - Conditional/if tags remained balanced (`conditional 4/4`, `if 5/5`).
        - VS Code diagnostics reported no XML errors.
        - Confirmed collapsed Seeds parity markers present in compat block (`checklistSeeds @pos=970,-110`, `zoomedSeeds checklist rows=1`, title `/1`).
    - 2026-07-01 (strict parity correction for remove-order drift):
      - User reported strict-parity result still off.
      - Root pattern: compatibility block removed zoomed entries using ascending indexed xpaths (`entry[1]`, `entry[2]`, ...), which can skip nodes due index reflow during sequential removes.
      - Corrective action:
        - Switched strict-parity generation to atomic removals:
          - compact: `rect[starts-with(@name,'Section')]`
          - zoomed: `grid[@name='...']/entry`
        - Re-generated strict-parity artifact and re-applied to live Dishong windows compat block.
      - Post-fix structural checks:
        - XML parse passed.
        - Tag balance remained valid.
        - Appended node counts matched strict parity target:
          - Workstations compact/zoomed: `16/16`
          - Vehicles compact/zoomed: `5/5`
          - Seeds compact/zoomed: `1/1`
    - 2026-07-01 (overview lane-overlap correction, zoomed already correct):
      - User report: overview side wrong while zoomed side remained correct.
      - Root pattern: strict-parity import moved compact Seeds lane to dump position `970,-110` but compact Medical lane in compat remained baseline `970,-110`, causing lane overlap in overview.
      - Corrective action:
        - Restored compact Seeds lane position in live compat block to baseline lane:
          - `/windows/window[@name='Schematics']//rect[@name='checklistSeeds']/@pos` = `1090,-110`
        - Kept zoomed Seeds parity block unchanged.
      - Validation after correction:
        - XML parse passed.
        - Conditional/if tag counts remained balanced.
        - VS Code diagnostics reported no XML errors.
    - 2026-07-01 (overview column reorder correction):
      - User follow-up: visual state was close but overview column order still wrong while zoomed remained correct.
      - Applied compact column reorder in live Dishong compat block to match requested ordering:
        - `checklistMedical @pos` set to `1090,-110`
        - `checklistSeeds @pos` set to `970,-110`
      - Kept zoomed Seeds/Workstations/Vehicles strict-parity block unchanged.
      - Validation after reorder:
        - XML parse passed.
        - Conditional/if tags remained balanced.
        - VS Code diagnostics reported no XML errors.
    - 2026-07-01 (explicit vanilla PurpleBook live sync request):
      - User requested running vanilla PurpleBook generation and pushing live files, explicitly separate from compatibility mod work.
      - Executed source-of-truth generator with vanilla `Data/Config` inputs and live sync enabled:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/_DLL-Projects/AGF-PurpleBookGenerator-v0.0.1/Generator/SCRIPT-PurpleBookGenerator.py --progression "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/progression.xml" --qualityinfo "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/qualityinfo.xml" --items "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/items.xml" --blocks "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/blocks.xml" --recipes "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Data/Config/recipes.xml" --sync-game-mod --no-sync-activebuild --game-mod-windows "C:/Program Files (x86)/Steam/steamapps/common/7 Days To Die/Mods/AGF-HUDPlus-PurpleBook-v3.0.0/Config/XUi_InGame/windows.xml"`
      - Generator reported live sync for:
        - `Config/XUi_InGame/windows.xml`
        - `Config/XUi_InGame/xui.xml`
        - `Config/gameevents.xml`
        - `Config/buffs.xml`
        - `Config/Localization.csv`
      - Post-sync validation:
        - XML parse checks passed for all synced XML files.
        - Mojibake check passed on live localization (`MOJIBAKE_COUNT:0`).
    - 2026-07-02 (from-generator slot alignment hardening for Dishong compat):
      - Updated `SCRIPT-GenPurpleBookCompat.py` source-generator compare mode (`--dump-generate-purplebook-rewrite-from-generator`) to emit slot-alignment rewrites when top-strip category order differs between vanilla and dump outputs.
      - Added extraction + remap logic for:
        - `magazineDecor` entry identity fields (`itemIcon`, tooltip/labels, fill)
        - zoomed page slot-order mapping from `tabsContents` `TabSelectorTab` rect order
        - full zoomed slot content remap (title icon/tooltip/labels/fill + checklist entry remove/append) for mismatched slots
      - Added explicit medical layout metadata to support Seeds<->Medical slot swaps:
        - `craftingMedical` (`checklistMedical`, `zoomedMedical`, `checklistMedicalTitle`)
      - Validation run command:
        - `c:/GitHub/7D2D-Mods/.venv/Scripts/python.exe c:/GitHub/7D2D-Mods/SCRIPT-GenPurpleBookCompat.py --dump-generate-purplebook-rewrite-from-generator --dump-dir "C:/Users/rft30/AppData/Roaming/7DaysToDie/Saves/Dishong Challenge (3. Desert)/DTC Test2/ConfigsDump"`
      - Validation evidence from generated output:
        - `<!-- Auto-generated slot-alignment block: magazineDecor + zoomed slot remap -->`
        - `<!-- slot 17: remap craftingMedical slot to craftingSeeds content -->`
        - `<!-- slot 19: remap craftingSeeds slot to craftingMedical content -->`

## Do-Not-Do Notes
- Do not reintroduce hardcoded 01_Draft output assumptions in generator path defaults.
- Do not run sync-to-game flags by default while validating relocation/pathing changes.
- Do not edit 02_ActiveBuild or 03_ReleaseSource unless explicitly requested.
- Do not rely on manual live-game `Config/XUi_InGame/windows.xml` edits for fallback header behavior; those must be reflected in generator source.
- Do not assume plain `--sync-game-mod` targets `AGF-HUDPlus-PurpleBook-v2.0.1`; pass explicit `--game-mod-windows` for live HUDPlus PurpleBook sync.
- Do not hardcode `agf0PurpleBookButtonTooltip` to "Crafting List" when Purple Book naming is required for button UX/localization parity.
- Do not assume config-side progression fixes are live until `Config/buffs.xml` is explicitly synced to the same game-mod target as windows.xml.
- Do not trust manual category spot-checks alone; rely on generator contract pass (`[ok] buffs contract check passed`) before sync.
- Do not reintroduce static/manual `Config/buffs.xml` editing for PurpleBook category logic; make all category/threshold behavior changes in generator emission code.
- Do not seed non-crafting recalc preservation from already-generated `Config/buffs.xml` when original source backups are available.
- Do not apply one threshold rule globally: tiered rows and non-tiered rows require different runtime CVar semantics.
- Do not collapse AGF book runtime append and crafting runtime append into one combined `buffStatusCheck02` generation path.
- Do not map `checklistSchematicsTitle` to tooltip/localized mixed-case keys when header/title behavior is intended; use `agf0PurpleBookButton`.
- Do not remove `xuiSchematics` localization alias while EnhancedAGF title fallback is active; it prevents raw-key title rendering.
- Do not assume quality tiers are always 6 without validating against `qualityinfo.xml` first.
- Do not treat removed-entity attribution as authoritative from dump comments alone; removed entities can lack in-node markers and should be validated before generating mod conditionals.
- Do not ship threshold-only remaps for Dishong PurpleBook progression when section icon/tooltip order changed; generate/apply icon rewrites from dump progression as part of the same pass.
- Do not force fixed Seeds 2x5 slot sectioning when dump progression collapses non-quality unlock levels; keep Seeds grouped by unlock level so section semantics match non-quality rules.
- Do not reintroduce Cosmetic Lock Icon marker overlays (`scrap` itemtypeicon) into Purple Book generated output.
- Do not force single-row icon packing in generic zoomed single sections when icon count exceeds six; keep 6-per-row wrapping unless a section has explicit bespoke layout rules.
- Do not validate dump-specific UI behavior with default/vanilla generator inputs; use ConfigsDump inputs when checking Dishong live behavior.
- Do not rely on `set`-only xpaths for icon slots that may not exist in baseline (for example `itemIcon[2..8]` in collapsed Seeds rewrites); append missing nodes or replace whole nodes first.
- Do not use progression-only or changed-only rewrite blocks when the requirement is exact overwrite parity; use strict-parity remove+append from fullgen dump window nodes.
- Do not remove zoomed grid entries with ascending indexed xpaths (`entry[1]`, `entry[2]`, ...); use atomic `entry` removal (or strictly descending indexes) to avoid index-shift leftovers.
- Do not move compact Seeds lane position from baseline without also applying the corresponding compact Medical lane move; otherwise overview lanes can overlap while zoomed remains correct.
- When user requests overview column reorder parity, explicitly set both compact lanes (`checklistSeeds` and `checklistMedical`) together in the same compat pass; never change only one lane.
- Do not infer zoomed page identity from rect names alone when tabs are reordered; treat `tabsContents` slot order as authoritative and remap slot content (including title + checklist entries), not just names.
