Build the ConsoleOpacityMod project using Visual Studio or dotnet build.

After building, ConsoleOpacityMod.dll will appear in this folder.

Place the DLL in your mod folder to activate the configurable console opacity patch.